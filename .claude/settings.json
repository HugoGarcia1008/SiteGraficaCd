{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd /c/SiteGraficaCd && git add -A && git diff --cached --quiet || (git commit -m \"auto: update files\" && export PATH=\"$PATH:/c/Program Files/GitHub CLI\" && git push) 2>/dev/null || true",
            "statusMessage": "Sincronizando com GitHub..."
          }
        ]
      }
    ]
  }
}
